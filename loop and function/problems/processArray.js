/*
    Write a function `processArray` that accepts an array of numbers as an argument. 
    The function should return a new array where each even number is squared, 
    and each odd number is multiplied by 3.
    hint: use the map method
*/
function processArray(arr) {
  //you code here
}

let arr = [1, 2, 3, 4, 5];
const resultArray = processArray(arr);

console.log(resultArray); // [3, 4, 9, 16, 15]



